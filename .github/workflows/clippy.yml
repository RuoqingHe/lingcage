name: LingCage Clippy

on:
  pull_request:

jobs:
  clippy:
    name: Lint With Clippy
    uses: ./.github/workflows/per-commit.yml
    with:
      run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
